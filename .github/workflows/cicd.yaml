name: CICD

on:
  push:
    branches:
      -  'docker-build'
jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build Project
        run: mvn clean install -DskipTests
      - name: Login to docker hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      - name: Build Docker Images
        run: |

           docker build -t dhruthikonijeti/springboot .
      - name: Push Docker Images
        run: |

          docker push dhruthikonijeti/springboot:latest

  deploy:
    needs: build
    runs-on: [aws-ec2]
    steps:
      - name: Log Disk Space
        run: |
          df -h
          docker system prune -a -f
      - name: Pull Image from docker hub
        run: |
            docker pull dhruthikonijeti/springboot:latest

      - name: Delete old container
        run: |
            docker rm -f springboot-container || true
      
      - name: Run docker container
        run: docker run -d -p 8080:8080 --name springboot-container dhruthikonijeti/springboot


